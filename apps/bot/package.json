{
  "name": "@pulsorclip/bot",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "PulsorClip Telegram bot with guided media downloads, format selection, and admin startup notifications.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "encode-cookies": "tsx src/scripts/encode-cookies.ts"
  },
  "dependencies": {
    "@pulsorclip/core": "*",
    "node-cron": "^4.2.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.20.6"
  }
}
