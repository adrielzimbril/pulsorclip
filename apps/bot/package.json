{
  "name": "@pulsorclip/bot",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "PulsorClip Telegram bot powered by the shared downloader core.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pulsorclip/core": "*",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
